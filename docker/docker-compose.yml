version: '3'
services:
  train:
    build: .
    env_file: .env
    hostname: ${HOSTNAME}
    ipc: host
    volumes:
      - /tmp:/tmp
      - /mnt/lab/users/eywalker/data:/data
      - /mnt/scratch07/edgar_data/v1_likelihood:/external
